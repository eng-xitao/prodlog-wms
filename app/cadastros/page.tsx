'use client';
import { useEffect, useState } from 'react';
type Tipo='Clientes'|'Fornecedores'|'Transportadoras'; type Pessoa={id:number;tipo:Tipo;nome:string;documento:string;cidade:string;contato:string;status:string};
const base:Pessoa[]=[{id:1,tipo:'Clientes',nome:'Cliente A',documento:'00.000.000/0001-00',cidade:'São Paulo/SP',contato:'comercial@cliente.com',status:'Ativo'},{id:2,tipo:'Fornecedores',nome:'Fornecedor Alpha',documento:'11.111.111/0001-11',cidade:'Guarulhos/SP',contato:'compras@alpha.com',status:'Ativo'},{id:3,tipo:'Transportadoras',nome:'Transportadora XP',documento:'22.222.222/0001-22',cidade:'São Paulo/SP',contato:'operacao@xp.com',status:'Ativo'}];
export default function Cadastros(){const[tipo,setTipo]=useState<Tipo>('Clientes');const[rows,setRows]=useState<Pessoa[]>(base);const[modal,setModal]=useState(false);const[form,setForm]=useState({nome:'',documento:'',cidade:'',contato:''});useEffect(()=>{try{const x=localStorage.getItem('prodlog_cadastros');if(x)setRows(JSON.parse(x))}catch{}} ,[]);const save=()=>{if(!form.nome||!form.documento)return alert('Informe nome/razão social e CPF/CNPJ.');const next=[...rows,{id:Date.now(),tipo,...form,status:'Ativo'}];setRows(next);localStorage.setItem('prodlog_cadastros',JSON.stringify(next));setForm({nome:'',documento:'',cidade:'',contato:''});setModal(false)};const remove=(id:number)=>{if(confirm('Excluir este cadastro?')){const next=rows.filter(r=>r.id!==id);setRows(next);localStorage.setItem('prodlog_cadastros',JSON.stringify(next))}};const filtered=rows.filter(r=>r.tipo===tipo);return <main className="min-h-screen bg-slate-100 p-4 sm:p-8"><div className="max-w-7xl mx-auto space-y-6"><header className="flex flex-col sm:flex-row sm:justify-between gap-4"><div><div className="text-xs uppercase tracking-[.2em] text-blue-600 font-bold">ProdLog WMS • Cadastros</div><h1 className="text-3xl font-black mt-1">Clientes, Fornecedores e Transportadoras</h1><p className="text-slate-500 mt-1">Cadastros próprios da empresa/cliente do armazém.</p></div><div className="flex gap-2"><a href="/" className="border bg-white px-4 py-2.5 rounded-lg font-semibold">← WMS</a><button onClick={()=>setModal(true)} className="bg-blue-600 text-white px-4 py-2.5 rounded-lg font-semibold">+ Novo cadastro</button></div></header><div className="flex gap-2 overflow-x-auto">{(['Clientes','Fornecedores','Transportadoras'] as Tipo[]).map(x=><button key={x} onClick={()=>setTipo(x)} className={`px-4 py-2.5 rounded-lg font-semibold ${tipo===x?'bg-blue-600 text-white':'bg-white border'}`}>{x}</button>)}</div><section className="bg-white border rounded-2xl overflow-hidden"><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-slate-50"><tr>{['Nome/Razão social','CPF/CNPJ','Cidade/UF','Contato','Status','Ações'].map(h=><th key={h} className="text-left px-5 py-3">{h}</th>)}</tr></thead><tbody className="divide-y">{filtered.map(r=><tr key={r.id}><td className="px-5 py-4 font-semibold">{r.nome}</td><td className="px-5 py-4 font-mono">{r.documento}</td><td className="px-5 py-4">{r.cidade||'—'}</td><td className="px-5 py-4">{r.contato||'—'}</td><td className="px-5 py-4 text-emerald-700">{r.status}</td><td className="px-5 py-4"><button onClick={()=>remove(r.id)} className="text-rose-600 font-semibold">Excluir</button></td></tr>)}{!filtered.length&&<tr><td colSpan={6} className="p-12 text-center text-slate-400">Nenhum cadastro.</td></tr>}</tbody></table></div></section>{modal&&<div className="fixed inset-0 z-50 bg-black/50 flex items-center justify-center p-4"><div className="bg-white rounded-2xl w-full max-w-xl p-6"><div className="flex justify-between"><h2 className="text-xl font-bold">Novo {tipo.slice(0,-1)}</h2><button onClick={()=>setModal(false)} className="text-2xl">×</button></div><div className="grid sm:grid-cols-2 gap-4 mt-5">{[['nome','Nome / Razão social'],['documento','CPF / CNPJ'],['cidade','Cidade / UF'],['contato','Telefone / E-mail']].map(([k,l])=><label key={k} className="text-sm font-medium text-slate-600">{l}<input value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})} className="mt-1 w-full border rounded-lg px-3 py-2.5" /></label>)}</div><div className="flex justify-end gap-2 mt-6"><button onClick={()=>setModal(false)} className="border px-4 py-2 rounded-lg">Cancelar</button><button onClick={save} className="bg-blue-600 text-white px-4 py-2 rounded-lg font-semibold">Salvar cadastro</button></div></div></div>}</div></main>}