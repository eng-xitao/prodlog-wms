import { useEffect,useState } from "react";
import { supabase } from "../lib/supabaseClient";
import { useAuth } from "../lib/AuthContext";
const STATUS_LABEL={pendente:"Pendente",separando:"Separando",separado:"Separado",expedido:"Expedido",cancelado:"Cancelado"};const STATUS_COLOR={pendente:"var(--amber)",separando:"#2563EB",separado:"var(--green)",expedido:"var(--text-dim)",cancelado:"var(--red)"};
export default function PickingPage(){const{company}=useAuth();const[customers,setCustomers]=useState([]);const[products,setProducts]=useState([]);const[warehouses,setWarehouses]=useState([]);const[orders,setOrders]=useState([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[customerId,setCustomerId]=useState("");const[warehouseId,setWarehouseId]=useState("");const[items,setItems]=useState([{productId:"",quantity:""}]);const[saving,setSaving]=useState(false);const[actionId,setActionId]=useState(null);const[orderWarehouses,setOrderWarehouses]=useState({});
async function loadAll(){setLoading(true);setError("");try{const[{data:c,error:e1},{data:p,error:e2},{data:w,error:e3},{data:o,error:e4}]=await Promise.all([supabase.from("customers").select("id, code, name").order("name"),supabase.from("products").select("id, sku, name, unit").order("name"),supabase.from("warehouses").select("id, name, location").eq("company_id",company.id).order("name"),supabase.from("wms_orders").select("id, code, status, source, sales_order_id, created_at, customers:customer_id (name), wms_order_items (id, product_id, quantity, quantity_picked, quantity_reserved, reservation_warehouse_id, products:product_id (sku, name, unit))").in("status",["pendente","separando","separado"]).order("created_at",{ascending:false}).limit(50)]);const e=e1||e2||e3||e4;if(e)throw e;setCustomers(c??[]);setProducts(p??[]);setWarehouses(w??[]);setOrders(o??[]);const selected={};(o??[]).forEach(order=>{const wh=order.wms_order_items?.find(i=>i.reservation_warehouse_id)?.reservation_warehouse_id;if(wh)selected[order.id]=wh});setOrderWarehouses(prev=>({...selected,...prev}))}catch(err){setError("Não foi possível carregar a tela: "+(err.message??"erro desconhecido"))}finally{setLoading(false)}}useEffect(()=>{if(company?.id)loadAll()},[company?.id]);
function updateItem(i,f,v){setItems(p=>p.map((x,n)=>n===i?{...x,[f]:v}:x))}function addItemRow(){setItems(p=>[...p,{productId:"",quantity:""}])}function removeItemRow(i){setItems(p=>p.filter((_,n)=>n!==i))}
async function handleCreate(e){e.preventDefault();setError("");const valid=items.filter(x=>x.productId&&Number(x.quantity)>0);if(!company?.id||!warehouseId||valid.length===0){setError("Selecione o almoxarifado e adicione pelo menos um item com quantidade.");return}if(valid.length!==new Set(valid.map(x=>x.productId)).size){setError("O mesmo produto não pode ser lançado duas vezes no mesmo pedido.");return}setSaving(true);try{const{data:code,error:ce}=await supabase.rpc("next_wms_order_code",{p_company_id:company.id});if(ce)throw ce;const{data:o,error:oe}=await supabase.from("wms_orders").insert({company_id:company.id,code,customer_id:customerId||null,status:"pendente",source:"prodlog"}).select("id").single();if(oe)throw oe;const{error:ie}=await supabase.from("wms_order_items").insert(valid.map(x=>({company_id:company.id,order_id:o.id,product_id:x.productId,quantity:Number(x.quantity),quantity_picked:0,quantity_reserved:0,reservation_warehouse_id:warehouseId})));if(ie){await supabase.from("wms_orders").delete().eq("id",o.id);throw ie}const{error:re}=await supabase.rpc("reserve_wms_order_stock",{p_wms_order_id:o.id,p_warehouse_id:warehouseId});if(re){await supabase.from("wms_orders").delete().eq("id",o.id);throw re}setCustomerId("");setWarehouseId("");setItems([{productId:"",quantity:""}]);await loadAll()}catch(err){setError("Não foi possível criar/reservar o pedido: "+(err.message??"erro desconhecido"))}finally{setSaving(false)}}
async function reserveOrder(orderId){setError("");setActionId(orderId);try{const wh=orderWarehouses[orderId];if(!wh)throw new Error("Selecione o almoxarifado antes de reservar o pedido.");const{error}=await supabase.rpc("reserve_wms_order_stock",{p_wms_order_id:orderId,p_warehouse_id:wh});if(error)throw error;await loadAll()}catch(err){setError(err.message??"Não foi possível reservar o estoque")}finally{setActionId(null)}}
async function setItemPicked(item,orderId,allItems){setError("");setActionId(item.id);try{if(Number(item.quantity_reserved||0)<Number(item.quantity))throw new Error("Este item ainda não está totalmente reservado. Reserve o pedido antes do picking.");const requested=Number(item.quantity);const picked=Number(item.quantity_picked||0);if(requested-picked<=0)return;const{error:ie}=await supabase.from("wms_order_items").update({quantity_picked:requested}).eq("id",item.id).eq("order_id",orderId).eq("company_id",company.id).gte("quantity_reserved",requested);if(ie)throw ie;const allDone=allItems.every(it=>it.id===item.id?requested>=Number(it.quantity):Number(it.quantity_picked||0)>=Number(it.quantity));const{error:oe}=await supabase.from("wms_orders").update({status:allDone?"separado":"separando"}).eq("id",orderId).eq("company_id",company.id).in("status",["pendente","separando"]);if(oe)throw oe;await loadAll()}catch(err){setError(err.message??"Não foi possível separar o item.")}finally{setActionId(null)}}
return <div><header style={{marginBottom:20}}><h1 style={styles.title}>Picking / Separação</h1><p style={styles.subtitle}>Primeiro reserve o estoque; depois realize a separação.</p></header>{error&&<div style={styles.error}>{error}</div>}<form onSubmit={handleCreate} style={styles.form}><p style={styles.formTitle}>Novo pedido de saída</p><select style={styles.input} value={warehouseId} onChange={e=>setWarehouseId(e.target.value)} required><option value="">Almoxarifado para reserva...</option>{warehouses.map(w=><option key={w.id} value={w.id}>{w.name}{w.location?` — ${w.location}`:""}</option>)}</select><select style={styles.input} value={customerId} onChange={e=>setCustomerId(e.target.value)}><option value="">Cliente (opcional)...</option>{customers.map(c=><option key={c.id} value={c.id}>{c.code} — {c.name}</option>)}</select>{items.map((it,i)=><div key={i} style={styles.itemRow}><select style={styles.input} value={it.productId} onChange={e=>updateItem(i,"productId",e.target.value)} required><option value="">Produto...</option>{products.map(p=><option key={p.id} value={p.id}>{p.sku} — {p.name}</option>)}</select><input style={{...styles.input,width:110}} type="number" min="0.000001" step="any" placeholder="Qtd." value={it.quantity} onChange={e=>updateItem(i,"quantity",e.target.value)} required/>{items.length>1&&<button type="button" style={styles.removeBtn} onClick={()=>removeItemRow(i)}>✕</button>}</div>)}<button type="button" style={styles.addItemBtn} onClick={addItemRow}>+ Adicionar item</button><button style={styles.saveBtn} type="submit" disabled={saving}>{saving?"Reservando...":"Criar e reservar"}</button></form><h2 style={styles.title2}>Pedidos em aberto</h2>{loading?<p style={styles.dim}>Carregando...</p>:orders.length===0?<p style={styles.dim}>Nenhum pedido em aberto.</p>:<div style={styles.list}>{orders.map(o=>{const reserved=(o.wms_order_items??[]).every(it=>Number(it.quantity_reserved||0)>=Number(it.quantity));return <div key={o.id} style={styles.card}><div style={styles.cardHeader}><span style={styles.recCode}>{o.code}</span><span style={{...styles.statusBadge,color:STATUS_COLOR[o.status]}}>{STATUS_LABEL[o.status]}</span></div><p style={styles.dim}>{o.customers?.name??"Sem cliente"} · Origem: <strong>{o.source==="prodos"?"ProdOS":"ProdLog"}</strong></p>{!reserved&&<div style={styles.reserveRow}><select style={styles.input} value={orderWarehouses[o.id]||""} onChange={e=>setOrderWarehouses(p=>({...p,[o.id]:e.target.value}))}><option value="">Selecionar almoxarifado...</option>{warehouses.map(w=><option key={w.id} value={w.id}>{w.name}{w.location?` — ${w.location}`:""}</option>)}</select><button type="button" style={styles.reserveBtn} disabled={actionId===o.id} onClick={()=>reserveOrder(o.id)}>{actionId===o.id?"Reservando...":"Reservar estoque"}</button></div>}<ul style={styles.itemsList}>{(o.wms_order_items??[]).map(it=>{const done=Number(it.quantity_picked||0)>=Number(it.quantity);return <li key={it.id} style={styles.pickRow}><span style={done?styles.itemDone:styles.dim}>{it.products?.sku} — {it.products?.name}: {it.quantity_picked||0}/{it.quantity} {it.products?.unit} · Reserva: {Math.min(Number(it.quantity_reserved||0),Number(it.quantity))}/{it.quantity}</span>{reserved&&!done&&<button style={styles.pickBtn} disabled={actionId===it.id} onClick={()=>setItemPicked(it,o.id,o.wms_order_items)} type="button">{actionId===it.id?"Separando...":"Marcar separado"}</button>}</li>})}</ul></div>})}</div>}</div>}
const styles={title:{fontFamily:"var(--font-display)",fontSize:22,margin:0},title2:{fontFamily:"var(--font-display)",fontSize:16,margin:"0 0 12px"},subtitle:{color:"var(--text-dim)",fontSize:13,margin:"6px 0 0"},dim:{color:"var(--text-dim)",fontSize:12.5},itemDone:{color:"var(--green)",fontSize:12.5,textDecoration:"line-through"},form:{display:"flex",flexDirection:"column",gap:12,background:"var(--panel)",border:"1px solid var(--line)",borderRadius:"var(--radius)",padding:20,marginBottom:28,maxWidth:680},formTitle:{fontSize:13,fontWeight:700,color:"var(--text-dim)",textTransform:"uppercase",letterSpacing:".04em",margin:"0 0 4px"},itemRow:{display:"flex",gap:8},input:{flex:1,minWidth:140,background:"var(--panel-2)",border:"1px solid var(--line)",borderRadius:"var(--radius)",padding:"9px 10px",color:"var(--text)",fontSize:13},removeBtn:{background:"transparent",border:"1px solid var(--line)",color:"var(--red)",borderRadius:"var(--radius)",width:36,cursor:"pointer"},addItemBtn:{alignSelf:"flex-start",background:"transparent",border:"none",color:"var(--amber)",fontSize:12.5,fontWeight:700,cursor:"pointer",padding:0},saveBtn:{background:"var(--amber)",color:"#fff",border:"none",borderRadius:"var(--radius)",padding:"10px 0",fontWeight:700,fontSize:13,cursor:"pointer",marginTop:6},reserveRow:{display:"flex",gap:8,alignItems:"center",margin:"10px 0"},reserveBtn:{background:"var(--amber)",color:"#fff",border:"none",borderRadius:"var(--radius)",padding:"9px 12px",fontSize:12,fontWeight:700,cursor:"pointer",whiteSpace:"nowrap"},list:{display:"flex",flexDirection:"column",gap:12,maxWidth:820},card:{background:"var(--panel)",border:"1px solid var(--line)",borderRadius:"var(--radius)",padding:16},cardHeader:{display:"flex",justifyContent:"space-between",marginBottom:4},recCode:{fontWeight:700,fontSize:14},statusBadge:{fontSize:12,fontWeight:700},itemsList:{margin:"8px 0 0",paddingLeft:0,listStyle:"none",display:"flex",flexDirection:"column",gap:6},pickRow:{display:"flex",justifyContent:"space-between",alignItems:"center",gap:8},pickBtn:{background:"var(--green)",color:"#fff",border:"none",borderRadius:"var(--radius)",padding:"4px 10px",fontSize:11.5,fontWeight:700,cursor:"pointer",whiteSpace:"nowrap"},error:{background:"var(--red-dim)",border:"1px solid var(--red)",color:"var(--red)",borderRadius:"var(--radius)",padding:"10px 12px",fontSize:13,marginBottom:16,maxWidth:820}};
